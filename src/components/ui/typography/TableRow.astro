---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"tr"> {}
const { class: className, ...props } = Astro.props;
---

<tr class:list={["m-0 border-t p-0 even:bg-muted", className]} {...props}>
	<slot />
</tr>
