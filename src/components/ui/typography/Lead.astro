---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"p"> {}
const { class: className, ...props } = Astro.props;
---

<p class:list={["text-muted-foreground text-base 2xl:text-xl", className]} {...props}>
	<slot />
</p>
